// // for each
const numbers = [1, 2, 3, 4];
// numbers.forEach((num) => console.log(num * 2));

// // map
// const mapEx = numbers.map((num) => num * 2);
// console.log(mapEx);

// //filter
const exFilter = numbers.every((num) => num > 2);
console.log(exFilter);
